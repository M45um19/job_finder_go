root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main.exe ./cmd/api"
  
  bin = "tmp/main.exe"
  
  include_ext = ["go", "tpl", "tmpl", "html", "env"]
  
  exclude_dir = ["assets", "tmp", "vendor"]

[log]
  time = true